// Também existe o operador ternário ? : para condições simples:

let status = (idade >= 18) ? "Adulto" : "Menor" // "Adulto" se idade >= 18, senão "Menor"

// O ternário testa a condição e retorna um valor conforme o resultado. 

// if/else: verifica uma expressão booleana e executa blocos diferentes.
// switch: faz comparações de igualdade múltiplas; útil para muitos casos discretos.
// ternário ? :: atalho para if/else simples, útil em atribuições.Essas estruturas permitem controlar o fluxo do programa com base em condições.