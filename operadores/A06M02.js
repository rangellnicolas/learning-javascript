// Ternário (condicional): condicao ? expr1 : expr2. Retorna expr1 se condicao for verdadeira, senão expr2. Exemplo: let status = (idade >= 18) ? "adulto" : "menor";.

let idade = 20;
let status = (idade >= 18) ? "adulto" : "menor"; // "adulto"

let nota = 7;
let resultado = (nota >= 6) ? "Aprovado" : "Reprovado"; // "Aprovado"

let numero = 5;
let parOuImpar = (numero % 2 === 0) ? "Par" : "Ímpar"; // "Ímpar"
