// Outros: operadores de string (+ concatena strings, ex.: "a" + "b" -> "ab"), operadores bitwise (&, |, etc.) e o operador spread/rest (...) em arrays e objetos.

// Concatenação de strings com +
let saudacao = "Olá, " + "mundo!"; // "Olá, mundo!"

let nome = "Ana";
let mensagem = "Bem-vinda, " + nome + "!"; // "Bem-vinda, Ana!"

// Concatenação com +=
let frase = "JavaScript";
frase += " é legal!"; //
