// Strings representam texto. 
// Declare-as em aspas simples, duplas ou crases para template literals. Exemplo:

let saudacao = "Olá, mundo!"; // String com aspas duplas
let nome = 'Nicolas'; // String com aspas simples   
let mensagem = `Bem-vindo, ${nome}!`; // Template literal com crase

// Concatene strings com o operador + ou use template strings: `Olá, ${nome}`. 
// Verifique o comprimento com .length, ou acesse caracteres por índice: str[0] é o primeiro caractere. Por exemplo:


let comprimento = saudacao.length; // 13
let primeiroCaractere = saudacao[0]; // "O"